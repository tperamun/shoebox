function getSignUpData(){
	  var form_element = document.getElementById("sign-up-form");
	  //alert(form_element.length);
    
    	var email= form_element.elements[0];
    	var username = form_element.elements[1];
    	var password = form_element.elements[2];
    	
    	//alert(email.value+" " + username.value+" " + password.value);
}
